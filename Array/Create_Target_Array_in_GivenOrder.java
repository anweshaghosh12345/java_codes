package Array;

import java.util.Arrays;

public class Create_Target_Array_in_GivenOrder {
    public int[] createTargetArray(int[] nums, int[] index) {
        int target[] =new int[nums.length];
        Arrays.fill(target,-1);
        for(int i=0;i<nums.length;i++){
            
        }
        return target;
    }
}
